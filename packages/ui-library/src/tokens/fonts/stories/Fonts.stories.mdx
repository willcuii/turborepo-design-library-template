import { Meta, Typeset } from '@storybook/blocks';
import { fonts } from '../fonts';

<Meta title="Tokens/Fonts" />

export const SampleText = 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.';

# 🔡 Fonts

## Heading

**Font:** Sofia-Pro, sans-serif

**Weights:** 300(light), 400(regular), 500(medium), 600(semi-bold), 700(bold), 900(black)

<Typeset
  fontSizes={[
    Number(fonts.size.h1),
    Number(fonts.size.h2),
    Number(fonts.size.h3),
    Number(fonts.size.h4),
    Number(fonts.size.h5),
    Number(fonts.size.h6)
  ]}
  fontWeight={fonts.weight.black}
  sampleText={SampleText}
  fontFamily={fonts.type.heading}
/>

## Body Content

**Font:** Lato, sans-serif

**Weights:** 100(thin), 300(light), 400(regular), 700(bold), 900(black)

<Typeset
  fontSizes={[ Number(fonts.size.text) ]}
  fontWeight={fonts.weight.regular}
  sampleText={SampleText}
  fontFamily={fonts.type.primary}
/>

## Code

**Font:** SFMono-Regular, Consolas,

<Typeset
  fontSizes={[ Number(fonts.size.text) ]}
  fontWeight={fonts.weight.regular}
  sampleText={SampleText}
  fontFamily={fonts.type.code}
/>
